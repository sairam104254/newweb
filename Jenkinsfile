pipeline {
triggers {
    pollSCM('') // Enabling being build on Push
  }
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
  }
}
